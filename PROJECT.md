# PROJECT — Conductor Index

> GENERATED by the `pm` plugin — do not hand-edit. Source of truth is
> `.conductor/state.json` (ordering, detours, links) + OpenSpec `tasks.md` (stories).
> Regenerate: `/pm:status` (or `node scripts/conductor.mjs render`).
> Last rendered: 2026-08-24T22:39:40.762Z

## Now

**`content-format-round-trip`** — Fix content round-trip: $1 converter bug, dual-format reads, safe section-scoped + whole-page writes (epic, P0) — 0/52 stories

## Detour stack

_Empty — no work is paused._

## Epics

| Priority | Epic | Lane | Role | Status | Progress | Links |
|----------|------|------|------|--------|----------|-------|
| P0 | `content-format-round-trip` | openspec | epic | active | 0/52 stories | - |
| P1 | `content-validator` | openspec | epic | queued ⚠ no change on disk | ⚠ tasks.md missing | depends-on→content-format-round-trip |
| P1 | `deps-security-sweep` | superpowers | epic | queued | — | - |
| P1 | `tool-doc-overhaul` | superpowers | epic | queued | — | depends-on→content-format-round-trip |
| P2 | `test-coverage-baseline` | superpowers | epic | queued | — | - |
| P2 | `repair-corrupted-pages` | claude-code | epic | planned | — | depends-on→content-format-round-trip |
| P2 | `repo-maintainer-cleanup` | claude-code | epic | queued | — | - |

## Recent detours

_None logged._

## Briefing (what a fresh session sees)

```
CONDUCTOR STATE — where we are and what's next

NOW: `content-format-round-trip` (openspec, epic, P0) — 0/52 stories

NEXT UP (by priority, then lane):
  • `deps-security-sweep` (P1, superpowers, queued) — —
  • `tool-doc-overhaul` (P1, superpowers, queued) — —
  • `test-coverage-baseline` (P2, superpowers, queued) — —
  • `repo-maintainer-cleanup` (P2, claude-code, queued) — —
  lanes: openspec 1 · superpowers 3 · claude-code 1

planned: 1 — see PROJECT.md

EPIC LINKS:
  • `content-validator` depends-on `content-format-round-trip`
  • `tool-doc-overhaul` depends-on `content-format-round-trip`
  • `repair-corrupted-pages` depends-on `content-format-round-trip`

RULES (pm): classify detours before fixing — minimal → fix+commit then `/pm:detour --minimal`; substantial → `/pm:detour` (own proposal + PUSH). After any state change, `/pm:status`. Resume via `/pm:resume` + reconcile gate. Mirror every PUSH/POP to a one-line Honcho memory.

Manage with /pm:status · /pm:next · /pm:detour · /pm:resume, or the `conductor` skill.
```
