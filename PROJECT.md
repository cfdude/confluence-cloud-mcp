# PROJECT — Conductor Index

> GENERATED by the `pm` plugin — do not hand-edit. Source of truth is
> `.conductor/state.json` (ordering, detours, links) + OpenSpec `tasks.md` (stories).
> Regenerate: `/pm:status` (or `node scripts/conductor.mjs render`).
> Last rendered: 2026-09-06T20:46:34.840Z

## Now

_No active epic set._

## Detour stack

_Empty — no work is paused._

## Epics

> Progress is claimed completion — ticked checkboxes, not verified delivery.

| Priority | Epic | Lane | Role | Status | Progress | Links |
|----------|------|------|------|--------|----------|-------|
| P0 | `content-format-round-trip` | openspec | epic | archived · delivered 🤖 | — | - |
| P1 | `content-validator` | openspec | epic | archived · delivered | — | depends-on→content-format-round-trip |
| P1 | `client-error-mapping` | superpowers | epic | archived · unknown | — | - |
| P1 | `deps-security-sweep` | superpowers | epic | archived · unknown | — | - |
| P1 | `tool-doc-overhaul` | superpowers | epic | archived · unknown | — | depends-on→content-format-round-trip |
| P1 | `transport-and-client-coverage` | superpowers | epic | archived · unknown | — | - |
| P2 | `test-coverage-baseline` | superpowers | epic | archived · unknown | — | - |
| P2 | `page-status-type-fix` | claude-code | epic | archived · unknown | — | - |
| P2 | `reenable-security-scans` | claude-code | epic | archived · unknown | — | - |
| P2 | `repair-corrupted-pages` | claude-code | epic | archived · unknown | — | depends-on→content-format-round-trip |
| P2 | `repair-pages-manual-merge` | claude-code | epic | queued | — | depends-on→repair-corrupted-pages |
| P2 | `repo-maintainer-cleanup` | claude-code | epic | archived · unknown | — | - |
| P3 | `set-content-property-decision` | decision | epic | queued | — | - |

## Dispositions

| Epic | Outcome | Recorded | Why |
|------|---------|----------|-----|
| `content-format-round-trip` | delivered | 2026-08-25T16:49:39.962Z | — |
| `content-validator` | delivered | 2026-08-25T20:00:33.692Z | — |

## Gate reviews

| Epic | Gate 1 (spec) | Gate 2 (implementation) |
|------|---------------|-------------------------|
| `content-format-round-trip` | pass (⚠ no checkable evidence) ⚠ unverifiable | pass (⚠ no checkable evidence) ⚠ unverifiable |
| `content-validator` | — | pass (⚠ no checkable evidence) ⚠ unverifiable |

## Recent detours

| When | SHA | Kind | Epic | Note |
|------|-----|------|------|------|
| 2026-08-25T15:59:57.861Z | `8e8a9a7` | MINIMAL | `content-format-round-trip` | fixed search_confluence_pages: new URL() on relative _links.next threw Invalid URL, discarding every successful search |
| 2026-08-25T15:59:57.948Z | `8e8a9a7` | AUTO-DETOUR | `content-format-round-trip` | fix(search): repair search_confluence_pages, broken by relative pagination link |

## Briefing (what a fresh session sees)

```
⚠ OpenSpec drift — this project's generated instruction files are 1.11.0, the installed CLI is 1.12.0. Run `openspec update` in a terminal; pm never runs it for you.
   It rewrites .claude/skills/openspec-* and .claude/commands/opsx/ in place, and local edits there are overwritten silently — never accept it blind.
   Those files are NOT git-tracked here, so the run leaves no diff to review — copy them aside first, then compare afterwards.

CONDUCTOR STATE — where we are and what's next
Progress is claimed completion — ticked checkboxes, not verified delivery.

NOW: (no active epic set)

NEXT UP (by priority, then lane):
  • `repair-pages-manual-merge` (P2, claude-code, queued) — —
  • `set-content-property-decision` (P3, decision, queued) — —
  lanes: openspec 2 · superpowers 5 · claude-code 5 · decision 1

DISPOSITIONS (recorded outcomes):
  • `content-format-round-trip` — delivered (2026-08-25)
  • `content-validator` — delivered (2026-08-25)

GATE REVIEWS:
  • `content-format-round-trip` gate 1: pass (⚠ no checkable evidence) ⚠ unverifiable · gate 2: pass (⚠ no checkable evidence) ⚠ unverifiable
  • `content-validator` gate 1: — · gate 2: pass (⚠ no checkable evidence) ⚠ unverifiable

EPIC LINKS:
  • `content-validator` depends-on `content-format-round-trip`
  • `tool-doc-overhaul` depends-on `content-format-round-trip`
  • `repair-corrupted-pages` depends-on `content-format-round-trip`
  • `repair-pages-manual-merge` depends-on `repair-corrupted-pages`

LIFECYCLE TASKS (pm): a task that is bookkeeping about the change's own lifecycle — above all the one that archives the change itself, which always qualifies — carries the literal `<!-- pm:lifecycle -->` on its task line. Mark it when the source is authored OR AMENDED; a source predating this capability gets the marker the first time you touch it.

RULES (pm): classify detours before fixing — minimal → fix+commit then `/pm:detour --minimal`; substantial → `/pm:detour` (own proposal + PUSH). After any state change, `/pm:status`. Resume via `/pm:resume` + reconcile gate. Mirror every PUSH/POP to a one-line Honcho memory.

Manage with /pm:status · /pm:next · /pm:detour · /pm:resume, or the `conductor` skill.
```
